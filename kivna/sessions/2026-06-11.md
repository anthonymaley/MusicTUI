# Session 2026-06-11

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date; tag v2.0.0 pushed, release published)

## What Was Done

- **2.0.0 (breaking) — slash commands trimmed to transport-only (14 → 10).** Removed `/music:search`, `/music:add`, `/music:similar`, `/music:playlist` after a lane analysis: slash = instant deterministic transport with osascript fallback (the plugin's entire zero-setup story), skill = composition (natural language), TUI = interactive browsing. The surviving set is exactly the no-auth tier of the auth matrix. CLI untouched — all 24 subcommands work, including ResultCache index chaining. README/guide rewritten around the lane split; suite 112 green; CLI rebuilt to 2.0.0; versions synced in all four locations.
- **Skill description hardened:** now states it is the composition layer ("slash commands cover transport only, so ALL search, library, playlist, and discovery requests come here") and adds the trigger phrases the removed commands used to catch. README gained the lane note + a Kid A multi-room example.
- **GitHub release v2.0.0 published** (rolls up the untagged 1.17.0), verified Latest with a clean body — notes written to a file and passed via `--notes-file`, sidestepping the 06-10 heredoc gotcha by construction.
- **Vault tidied (kivna save):** Status.md rewritten into Where We Are / What's Open / What's Next (release-by-release stack compressed; detail stays in playbook); MOC Quick Commands table replaced with transport + CLI-composition tables, stale install identifiers fixed (`music@anthonymaley-music` → `music@apple-music-marketplace`, repo `anthonymaley/music` → `anthonymaley/apple-music`), CLI cache path corrected; Weekly.md created (week of 2026-06-08, rolling achievements/risks).
- **User statusline fixed properly after a wrong prescription.** I had told the user to bump a version segment in `~/.claude/settings.json` — sourced from the README, without reading their file. Reality: their statusline was a personal ctx-free wrapper with no plugin path at all. Wrapper now composes now-playing + ctx-free, pointing at the repo copy of `scripts/statusline.sh` (immune to plugin-cache version churn). Verified live with a paused track; the Music-stopped branch is unexercised.

## What's Next

- **User to live-verify playing an APPLE-badged playlist** (`p`/`s`/Enter) — still the one untested 1.17.0 path; then delete the manual duplicates ("Loops (June)", "Nu Cumbia (Apple)").
- **Decide the `.gitignore` "Dev-only files" intent** and apply the tend fixes (delete dead entries + 15 `.DS_Store`s). The `kivna/` entry again forced `git add -f` for this session log.
- `/kerd:trim`: playbook Current Status now stacks 9 version entries; `docs/superpowers/` archived-in-place.
- New backlog candidates from this session: statusline cache-path shim (resolve newest version at runtime); vault audit (nothing checks the vault — the MOC identifiers were wrong for weeks).

## Key Decisions

- **Slash commands are transport-only; composition lives in skill/TUI/CLI.** The deciding argument: the transport set is the only zero-setup surface (osascript fallback) and TODO's "broad public push" goal stands; the compose commands needed CLI+auth anyway, so they got no benefit from the slash layer.
- **Removing public commands = major bump (2.0.0)**, communicated via `feat!:` + BREAKING CHANGE + a migration table in the release notes.
- **Doc follow-ups folded into 2.0.0 before tagging** rather than bumping again — the version wasn't published yet, so the tag includes them.
- **Plugin author's statusline points at the repo, not the plugin cache** — kills the version-segment churn the README documents for consumers.

## Commits

- `d6f9fca` feat(plugin)!: trim slash commands to transport-only — 14 → 10 (2.0.0) — pushed
- `1c0005b` docs: route compose triggers to the skill; README lane split + Kid A example — pushed
- Tag `v2.0.0` pushed; GitHub release published and verified Latest.

## Gotchas

- **`gh release view` has no `isLatest` JSON field** — use `gh release list` to confirm which release is Latest.

## Insights

- **The lane boundary equals the auth boundary:** every slash command that survived the trim is in the no-auth tier. When a product split lines up with an existing technical boundary like that, the cut is probably in the right place.
- **The vault drifts exactly like unread docs.** The MOC's install identifiers were wrong (pre-rename repo and marketplace names) and nothing ever caught it — tend audits the repo, nothing audits the vault. Same root cause as the README's three-generations-old key table found on 06-10: no feedback loop → maximum drift.
- **Prescriptions are claims about state** (shame point `read-the-state-before-prescribing`, persisted centrally + project memory): I told the user their settings.json needed a version bump based on the README's example, never having read their file; they acted on it. Closing-note extras are where verification discipline silently drops — read the target or phrase it conditionally.
